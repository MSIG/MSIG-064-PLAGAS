<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="msig.com.formularios_control">

    <uses-sdk
        android:minSdkVersion="7"
        android:targetSdkVersion="7" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <application
        android:allowBackup="true"
        android:icon="@drawable/img_icon"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ListadoFormularios"
            android:label="Listado de Formularios"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".formularios.FormularioChincheSalivosa"
            android:label="Chinche Salivosa"
            android:parentActivityName=".MainActivity" />
        <activity android:name=".detalles.AddDetalleChincheSalivosa" />
        <activity android:name=".formularios.VerMaestrosChincheSalivosa"
            android:label="Formularios Chinche Salivosa"
            android:parentActivityName=".MainActivity"/>
    </application>

</manifest>